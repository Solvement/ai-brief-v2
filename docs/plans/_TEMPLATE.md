# Plan: <任务名> (<ID>)

> 触发：**任何 >100 行改动、schema/数据契约/路由变更、新栏目/新范式、批量生成** 必须先写本 plan（RULES #11）。
> 完成后必须独立 agent 审，按 §1 大/小方向为标的（RULES #12）。

## 1. 大方向 / 小方向（验收标的）
- **大方向**：<这件事服务的产品/北极星目标，一句话——审核以此为最终标的>
- **小方向**：<本次具体要达成的可观察结果，bullet 化，每条可判定>

## 2. 需求（目标 / 非目标 / 影响范围 / 验收）
- 目标：
- 非目标：
- 影响范围（模块/数据/路由/用户）：
- 验收标准（可观察）：

## 3. 方案（怎么做）
- 改动点（file:line 级别尽量具体）：
- 数据契约/schema 变化（有则标 🔴 需 Kevin 签字）：

## 4. eval 方式（机器 DONE 怎么定义）
- 结构门（脚本/断言，免费）：
- 内容门（独立 agent + rubric）：
- 成功指标（真正证明"做对了"的 holdout/查询）：

## 5. tool 调用（种类 + 方式）
- 用哪些 tool / 脚本 / MCP：
- 是否新增依赖（默认不加重资产，RULES/SPEC）：

## 6. 编排方式（需不需要子 agent / 框架）
- 单主控够不够？需要拆角色吗（需求/方案/开发/审查/测试）？
- **不上 langraph/CrewAI/AutoGen**，除非要发线上自主多 agent 产品——理由：编排底座=Claude sub-agent + dynamic workflow + codex + 确定性脚本（见 docs/agents/README.md）。
- 若用子 agent，列派发表：角色 / eval / 模型 / effort（RULES #16）。
- 是否用 dynamic workflow（批量生成/审计/多角度/质量门 → 是）。

## 7. 切片（每片=可运行交付）
- 片 1（先证小样）：
- 片 2 … N：
- 每片的"可运行、Kevin 能看到"是什么：

## 8. 风险 / 回退
- 风险点：
- 回退方式（保留上一版）：
