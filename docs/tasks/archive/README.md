# Archived task specs

These task specs (000–017) drove the original Vite-prototype phase of AI-brief
and have either:

1. been delivered (the resulting code lives under `src/`), or
2. been superseded by `docs/handoff/codex-next-tasks.md`, which is the
   current source of truth for in-flight phases.

They are kept here as a historical record of how the product evolved, but
**do not start new work from this folder**. The active queue is:

- `docs/handoff/codex-next-tasks.md` — Phase 4, 5, … (next-up specs)
- `docs/image-policy.md` — current configuration of the image system
- `docs/implementation-plan.md` — long-range roadmap

If you find a task spec here whose code clearly needs follow-up, write a new,
focused spec under `docs/tasks/` (or fold it into the next `Phase` section of
the handoff doc) — don't resurrect the archived file in place.
