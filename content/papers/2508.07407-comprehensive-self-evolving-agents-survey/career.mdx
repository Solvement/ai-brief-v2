# Career & Learning · 求职与成长

> 假设你已读完 Paper 栏。这里只谈它对**应用型 AI 工程师 / FDE / AI-app 开发者**的你有什么用。

## 为什么这对我重要

如果说 `2507.21046` 给你"自进化的四个选择题(What/When/How/Where)",这篇给你三样直接能用的东西:**(a)一个可套的工程回路**(输入 → agent 系统 → 环境 → 优化器 → 改 agent 系统);**(b)一条发展路线(MOP→MOA→MAO→MASE)**帮你给自己的系统"定位现在在第几阶段";**(c)一组护栏次序(三定律:安全>性能>进化)**告诉你自改进时什么不能动。你做任何"会自我改进"的功能,都能往这个回路里填:**反馈从哪来(环境)、优化器改的是哪个对象(单 agent 的提示/记忆/工具,还是多 agent 的拓扑,还是某领域)、以及改的时候先保住什么(安全)**。

> [!key] 直接映射到我们的 L1
> 我们站的 L1:**输入**=每日候选;**agent 系统**=选稿+冷审流水线;**环境**=Kevin 的阅读/反馈 + 质量门;**优化器**=用这些反馈改"口味记忆 + 提示/连边"。按它的发展路线,我们正处在 **MAO(多 agent 编排)往 MASE(多 agent 自进化)过渡**:已经有多 agent 协作,缺的是"闭环自改进"。三定律给我们的护栏:**冷审/质量门(Endure 安全)优先于刷质量分(Excel),刷分又优先于自动改连边/提示(Evolve)**——不能为了进化绕过质量门。

## 该学的 AI 工程技能

| 技能 | 学什么 | 怎么练 |
| --- | --- | --- |
| 优化回路建模 | 把自改进系统拆成 输入/agent/环境/优化器 四件 | 给你的功能画这张四组件图,标清反馈从哪来、改什么 |
| 选对优化对象 | 单 agent(提示/记忆/工具)vs 多 agent(拓扑)vs 领域 | 先定"改哪个对象",再选方法,别一上来就微调 |
| 单 agent 自优化 | 提示(APE)、记忆、工具、训练(STaR/ToRA)各自怎么做 | 实现一个"按反馈迭代改提示"的最小优化器 |
| 多 agent 自动构建 | 自动搭/改多 agent 工作流与拓扑(接 MetaGPT/GPTSwarm) | 把一个固定多 agent 流程改成可被优化器调的 |
| 领域落地 + 评估 | 自进化在具体领域怎么用、怎么衡量 | 给你的领域选 1-2 个基准,做"进化前后"对比 |

## 系统设计心法

一句话:**把"自我改进"画成一个闭环(输入→agent→环境→优化器),先立护栏次序(安全>性能>进化),再决定优化器改哪个对象。**
- **先有反馈源(环境),才谈优化**:没有可靠反馈,优化器无从下手——先把"环境给什么信号"定清楚。
- **护栏次序刻在前面(三定律)**:**Endure(安全)> Excel(性能)> Evolve(进化)**——任何自动改动不能为了刷分牺牲安全/稳定。把这条做成系统里的硬约束(如:改动必须先过质量门才允许生效)。
- **选对象 > 选方法**:改提示/记忆(便宜)还是改拓扑/参数(贵),决定了成本与收益。
- **先给系统定阶段(MOP→MOA→MAO→MASE)**:你现在是"参数微调(MOA)"、"多 agent 编排(MAO)"还是"闭环自进化(MASE)"?定位清楚才知道下一步该补什么。
- **单 agent 与多 agent 是两层**:别把"调一个 agent"和"调一群 agent 怎么组织"混为一谈。
- **两把尺子叠用**:本篇(优化器+安全视角)+ 2507.21046(What/When/How/Where)一起,设计自进化最全。

## 作品集角度(Portfolio angle)

"自进化系统设计"是最前沿方向。两条路:① 用这张四组件回路,给一个系统做一条**完整的自改进闭环**(明确环境反馈 + 优化对象 + 评估),把"它怎么越用越强"讲清楚;② 给 `EvoAgentX/Awesome-Self-Evolving-Agents`(2.2k★)或 EvoAgentX 生态提 PR。能把"自进化回路 + 选对优化对象"讲明白,是很硬的体系化能力。

## 可造(Build ideas)

| 造什么 | 范围 | 时间 | 为什么有用 |
| --- | --- | --- | --- |
| 四组件自改进闭环 | 选一个功能,补齐 输入/agent/环境反馈/优化器,跑通一圈 | 1-2 周 | 把抽象回路变成能演示的小系统 |
| 提示优化器 | 按环境反馈迭代改提示(APE 思路),带评估 | 4-7 天 | 最便宜的单 agent 自优化,效果直观 |
| 多 agent 自动构建 | 把固定多 agent 流程改成可被优化器调拓扑 | 1-2 周 | 接 GPTSwarm/MetaGPT 那条线,讲深度 |
| 领域自进化 demo | 在一个领域(如代码)做"进化前后"对比 + 基准 | 2 周 | 把自进化落到真实领域 + 评估 |

## 简历 / 面试故事

诚实写法:
- *"Designed a self-improving system as a four-component optimisation loop (inputs → agent system → environment → optimiser), choosing the optimisation target (prompt/memory vs multi-agent topology) per cost/benefit."*
- *"Built a feedback-driven prompt optimiser with explicit environment signals and before/after evaluation."*
- *"(若做了 PR/Demo)Contributed to the EvoAgentX self-evolving-agents ecosystem / awesome-list."*

> [!key] 面试可讲
> "怎么设计一个会自我改进的 agent 系统?"——用这张回路答:**先立护栏次序(安全>性能>进化,即三定律),再定环境反馈源,再选优化器改哪个对象(单 agent 提示/记忆 vs 多 agent 拓扑 vs 领域),最后配评估闭环**;并点出"选对象比选方法更决定成本"、"自改进系统第一位是安全约束不是刷分"。能顺手用 MOP→MOA→MAO→MASE 给系统定位,更显体系化 + 工程取舍。

## 学习清单

**必学**
- 四组件优化回路(输入/agent/环境/优化器)
- 三定律护栏次序:Endure(安全)> Excel(性能)> Evolve(进化)
- 选优化对象:单 agent(提示/记忆/工具)vs 多 agent(拓扑)vs 领域
- 评估闭环(基于基准 + 基于 LLM + 安全/对齐/鲁棒性基准)

**有用**
- 单 agent 自优化方法(APE/STaR/ToRA)
- 多 agent 自动构建(MetaGPT/AutoGen/DSPy)

**可选**
- 四阶段发展路线 MOP→MOA→MAO→MASE(给自己的系统定阶段)
- 与 2507.21046(What/When/How/Where)两视角的叠用
