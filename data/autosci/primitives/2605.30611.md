<!-- AI-ONLY · AutoSci 自进化语料。不进 Kevin 的阅读 tab（人读的是 content/papers/.../paper.mdx + career.mdx）。 -->
# AutoSci reuse · 2605.30611 Crafter

## 相关度
**Indirect pattern(强)** —— 任务领域(科学配图)无关,但核心模式(带结构化记忆的 plan→render→diagnose→refine harness + 指令式批评家 + best-so-far 回退)可几乎原样搬成 AutoSci 的"自改进执行引擎"。可复用的是**控制结构,不是具体智能体**。

## 可复用原语
"引擎无关的验收-精修环 + 类型化结构记忆":把任何不可靠的一步生成(画图/写代码/提假设)包进循环——结构化规格 𝒮 当记忆(typed-edit 修改)、指令式批评家给"逐维度分+缺陷+修正"、写回 𝒮、非单调时回退到历史最优。即 AutoSci L1 自进化的骨架。

## 映射
| 论文 | AutoSci 模块 | 复用方式 |
| --- | --- | --- |
| 结构化规格 𝒮 + typed edit | Typed Memory | 结构化记忆+类型化操作替代长对话历史 |
| Designer 出 K 候选 | Hypothesis/Plan Proposer | 一次多候选先筛再执行,省预算 |
| Executor 现成引擎 | Experiment Runner | "跑一步实验/写代码"当可热插拔引擎 |
| 𝒱 指令式批评家 | Cold Critic / 质量门 | 逐维度+缺陷+修正,契合独立冷审计 |
| Convergence Judge + 回退 | Self-evolve 控制器 | 接受/继续/回退,防越改越差 |
| 早退门(两维≥6.5) | 预算/停机策略 | 便宜判据提前停省 token |

## 拟建模块 refine_harness
- purpose: 把任意"一步生成 engine"包成可迭代收敛、带结构化记忆、可观测的可靠步骤
- inputs: task_spec / engine / critic / budget(max_rounds T + 成本上限)
- outputs: best_artifact / final_spec 𝒮 / trace(每轮诊断+typed_edit)
- deps: Typed Memory、Cold Critic、LLM/工具调用层
- failure modes: 非单调发散(best-so-far 兜底)/ critic 自评偏置(需独立冷评委)/ 成本爆炸(硬预算+早退)/ typed-edit 表达力不足退化为自由文本

## 小实验(1-14 天)
任务:给研究问题让 engine 产"实验设计草案",critic 按 可行性/新颖性/可证伪性/资源 打分+指缺陷,refine 3 轮。
对照 A=自由文本改写 vs B=typed-edit 𝒮 + 指令式 critic + best-so-far。
指标:第 3 轮被独立冷评委判"可执行"比例;消融去掉结构化记忆/指令式 critic 各掉多少。
产出:refine_harness v0 + 消融表 → 进 AutoSci L1。

## 风险
评委同源偏置(必须独立冷评委)/ 成本 ~4×(硬预算+早退)/ 研究任务结构化规格难定义 / 别把图像专属智能体(SVG/SAM3)硬塞进来。
